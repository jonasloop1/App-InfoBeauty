<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_infobeauty">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppInfoBeauty">
        <activity android:name=".ServicosEmpresas"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".Maps_InfoBeauty"
            android:label="@string/title_activity_maps__info_beauty" />
        <activity
            android:name=".TelaNavegacao"
            android:label="@string/title_activity_tela_navegacao"
            android:theme="@style/Theme.AppInfoBeauty.NoActionBar" />
        <activity
            android:name=".TelaLoginUsuario"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaLoginEmpresa"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaCadastroUsuario"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaCadastroEmpresa"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaEscolhaEmpresa"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaEscolhaUsuario"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TelaInicial"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>