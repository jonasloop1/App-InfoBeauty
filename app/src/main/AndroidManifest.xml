<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_infobeauty">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppInfoBeauty">
        <activity android:name=".TelaLoginUsuario"></activity>
        <activity android:name=".TelaLoginEmpresa" />
        <activity android:name=".TelaCadastroUsuario" />
        <activity android:name=".TelaCadastroEmpresa" />
        <activity android:name=".TelaEscolhaEmpresa" />
        <activity android:name=".TelaEscolhaUsuario" />
        <activity android:name=".TelaInicial" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>